apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/issuer: selfsigned
  name: nginx
  namespace: dev-kpt # kpt-set: ${environment}-${tech}
spec:
  rules:
  - host: dev-kpt.largelyconjecture.com # kpt-set: ${environment}-${tech}.largelyconjecture.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: nginx
            port:
              number: 80
  tls:
  - hosts:
    - kpt-dev.largelyconjecture.com # kpt-set: ${tech}-${environment}.largelyconjecture.com
    secretName: cert-secret
